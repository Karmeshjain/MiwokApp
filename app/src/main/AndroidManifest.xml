<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miwikapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiwikApp">
        <activity android:name=".ColorsActivity" android:label="Colors"></activity>
        <activity android:name=".FamilyActivity" android:label="Family" />
        <activity android:name=".PhrasesActivity" android:label="Phrases" />
        <activity android:name=".NumberActivity" android:label="Numbers" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>